.worktrees/

*.BACKUP_*.docx

# Local pilot results are runtime outputs and must never be committed.
pilot/results/*.jsonl

# Project-local router trace databases are runtime outputs.
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal

# Local calibration outputs contain reproducibility artifacts and raw responses.
calibration/results/*
!calibration/results/.gitkeep
